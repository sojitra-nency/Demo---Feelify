import * as React from 'react';
import type { Metadata } from 'next';



export const metadata = { title: `Feelify | Google Authentication ` } satisfies Metadata;

export default function Page(): React.JSX.Element {
  return (      
        <p>Home</p>
  );
}
